version: '3.3'

services:
  mysql:
    image: mysql:5.7-oracle
    restart: always
    tty: true
    ports:
      - 3306:3306
    container_name: 'social_media_db_1'
    environment:
      MYSQL_DATABASE: social_media
      MYSQL_ROOT_PASSWORD: root
